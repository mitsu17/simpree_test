.newPost__container
  .newPost__container__title
    %h1 New Post
  .newPost__container__form
    .newPost__container__form__field
      = form_with(model: @post, local: true) do |form|
        = form.file_field :clip, :accept => 'video/*', placeholder: " ", class: :newPost__container__form__field__clip
        = form.text_field :title, placeholder: "title", class: :newPost__container__form__field__title
        = form.text_area :text, placeholder: "text", class: :newPost__container__form__field__text
        = form.text_field :category, placeholder: "category", class: :newPost__container__form__field__category
        = form.submit "Send", class: :newPost__container__form__field__btn


















<div class="newPost__container">
  <div class="newPost__container__title">
    <h1>New Post</h1>
  </div>
  <div class="newPost__form__container">
    <div class="newPost__form__field"> 
      <% form_with(model: @post, local: true) do |form| %>
        <% form.file_field :clip, :accept => 'video/*', placeholder: "ファイルを選択する", class: :newPost__form__field__clip %>
        <% form.text_field :title, placeholder: "title", class: :newPost__form__field__title %>
        <% form.text_area :text, placeholder: "text", class: :newPost__form__field__text %>
        <% form.text_field :category, placeholder: "category", class: :newPost__form__field__category %>
        <% form.submit "SEND" %>
      <% end %>
    </div>
  </div>
</div>


<div>
  <%= image_tag "video_file_icon_2.png", class: "newPost__form__field__clipImage" %>
</div>


<label for="newPost__form__field__clip">
  <%= link_to image_tag'video_file_icon_2.png', class : "newPost__form__field__clipImage") %>
  <%= form.file_field :clip, :accept => 'video/*', placeholder: "ファイルを選択する", id="newPost__form__field__clip" %>
</label>

= f.label :image, class: "input-box__image" do
  = icon("fas", "image")
  = f.file_field :image, class: "input-box__image__file"

%h1 New Post


.form__container
  .form__container__field
    = form_with(model: @post, local: true) do |form|

      = form.file_field :clip, :accept => 'video/*', placeholder: "ファイルを選択する"
      = form.text_field :title, placeholder: "title"
      = form.text_area :text, placeholder: "text"
      = form.text_field :category, placeholder: "category"
      = form.submit "SEND"
























<div class="detail__post">
  <div class="detail__post__clip">
    <%= video_tag(url_for(@post.clip), controls:'') %>
  </div>
  <div class="detail__post__title">
    <%= @post.title %>
  </div>
  <div class="detail__post__text">
    <%= simple_format @post.text %>
  </div>
  <div class="transition__btn">
    <%= link_to "Edit", class: "transition__btn__edit", users_path %>
    <%= link_to "Delete", class: "transition__btn__delete", users_path %>
  </div>
</div>









%div
  = video_tag(url_for(@post.clip), style:'width:300px; height: auto;', controls:'')


.postTitle
  = @post.title
.postText
  = simple_format @post.text







  %main
  .grid
    - @posts.each do |post|
      .grid__item
        = link_to video_tag(url_for(post.clip), class: 'grid__item__image'), "/posts/#{post.id}", method: :get
        %h3= post.title 
        %p= simple_format(post.text)
        .grid__item__menuBtn
          =link_to image_tag('menu_icon_4.png', class: 'grid__item__menuBtn__image'), root_path
